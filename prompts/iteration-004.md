# Iteration 004

## Prompts
- Update the decimal-numbers lesson so student-facing fractions are displayed as vertical stacked fractions, not slash notation like `1/2`.

## Output
- Replaced static slash-style fractions in the `decimals` HTML pages with the existing stacked fraction markup.
- Kept the dynamic drill formatter, which already converts slash-style fraction strings into stacked fractions when rendered.
